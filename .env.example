# =============================================================================
# Content Strategy Portal — Frontend Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# Firebase Configuration
# Get these values from Firebase Console > Project Settings > General
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id

# App Configuration
VITE_APP_NAME=SEO Checklist Pro
VITE_APP_URL=http://localhost:5173

# AI Proxy Server (required for AI features)
# Points to the BASE URL of the proxy server — do NOT include /api/claude
# For local development: http://localhost:3001
# For production: https://your-proxy.up.railway.app
# See server/.env.example for proxy server configuration (API keys, Firebase Admin, etc.)
VITE_AI_PROXY_URL=http://localhost:3001

# Site URL (used for SEO canonical URLs, sitemaps, and Open Graph tags)
# Default: https://contentstrategyportal.com
VITE_SITE_URL=https://contentstrategyportal.com

# DEPRECATED: Direct browser API key (insecure — use proxy server instead)
# VITE_CLAUDE_API_KEY=
