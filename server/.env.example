# =============================================================================
# Content Strategy Portal — AI Proxy Server Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# RAILWAY DEPLOYMENT NOTES:
#   - PORT is injected automatically by Railway (do NOT set it manually)
#   - All other variables below must be added in Railway > Service > Variables
#   - See railway.toml for build/deploy configuration
# =============================================================================

# Server
PORT=3001
NODE_ENV=production

# CORS — comma-separated list of allowed origins
# Include your Vercel production URL and localhost for development
ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:5173

# -----------------------------------------------------------------------------
# Firebase Admin SDK
# -----------------------------------------------------------------------------
# Used to verify Firebase auth tokens from the frontend.
# Get this from Firebase Console > Project Settings > Service Accounts > Generate new private key
# Paste the ENTIRE JSON file contents as a single-line string:
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"firebase-adminsdk-...@your-project.iam.gserviceaccount.com","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token"}

# -----------------------------------------------------------------------------
# LLM API Keys
# -----------------------------------------------------------------------------
# These keys are stored server-side only — never exposed to the browser.

# Anthropic Claude — https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-api03-...

# OpenAI GPT — https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Google Gemini — https://aistudio.google.com/apikey
GEMINI_API_KEY=AI...

# Perplexity Sonar (Phase 2 — not yet used)
# PERPLEXITY_API_KEY=pplx-...
